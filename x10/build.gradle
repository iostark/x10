apply plugin: 'com.android.library'

android {
    compileSdkVersion project.compileSdkVersion
    buildToolsVersion project.buildToolsVersion

    defaultConfig {
        minSdkVersion project.minSdkVersion
        targetSdkVersion project.targetSdkVersion
        versionCode project.x10VersionCode
        versionName project.x10VersionName
        buildConfigField "String", "SDK_SOURCE_CONTROL_HASH", "\"$project.gitCommitHash\""

        testInstrumentationRunner "android.support.test.runner.AndroidJUnitRunner"
    }
    buildTypes {
    }
}

apply from: project.x10LibraryGradle

dependencies {
    implementation project(':x10-base')
    implementation project(':x10-bitmap')
    implementation project(':x10-email')
    implementation project(':x10-file')
    implementation project(':x10-inputdevice')
    implementation project(':x10-media')
    implementation project(':x10-permission')
    implementation project(':x10-threading')
    implementation project(':x10-time')

    implementation project.dependencyComAndroidSupportAppCompatv7

    testImplementation project.dependencyJunit
}
